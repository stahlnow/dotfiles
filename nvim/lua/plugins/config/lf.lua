vim.keymap.set("n", "<leader>e", "<cmd>Lf<cr>", {noremap = true})

return {
  border = "single", -- border kind: single double shadow curved rounded
  escape_quit = true, -- map escape to the quit command (so it doesn't go into a meta normal mode)
}
